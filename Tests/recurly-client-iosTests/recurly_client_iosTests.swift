import XCTest
@testable import recurly_client_ios

final class recurly_client_iosTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(recurly_client_ios().text, "Hello, World!")
    }
}
